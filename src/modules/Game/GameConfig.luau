--!strict

local GameConfig = {
	NOTE_MAX_STRANGER_PER_SENDER_PER_DAY = 1,
	NOTE_MAX_VISIBLE_STRANGER = 5,
	NOTE_STRANGER_DECAY_HOURS = 24,

	LOCKER_INTERACT_DISTANCE = 10,
	LOCKER_DECOR_COOLDOWN_SECONDS = 0.5,

	HANDOFF_MAX_DISTANCE = 12,
	HANDOFF_COOLDOWN_SECONDS = 2,
	HANDOFF_OFFER_EXPIRE_SECONDS = 15,
	HANDOFF_VISUAL_DISPLAY_SECONDS = 4,

	ARCADE_QUEUE_MAX = 8,
	ARCADE_SPECTATOR_MAX = 12,
}

return GameConfig
